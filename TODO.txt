Micro optimizations:
Put hack ram back to 1.7 via:
interface NetworkServer extends Required<Server> {
  batcherRam: BigInt;
}

Next phase:
* Cloud servers up to 64GB are NOT softcapped
* Cloud servers buy equally
* Fix share takes too long bug (promise any?)


