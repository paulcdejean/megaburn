import type { NS } from "@ns";
import type { Network } from "../types";

// oxlint-disable-next-line no-unused-vars
export function oneThread(ns: NS, network: Network, target: string): number {
	return 1;
}
